# Agent the Hard Way in Bash

Generated with: Pi + minimax-m3 (Ollama Cloud)

LOC:

```
     245 agent.sh
```

Note that because bash is a bit special the spec was slightly different, thus included in this folder.
