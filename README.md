# Agent the Hard Way

Implementations of coding agents using same spec from scratch. Just to compare a bit how they look and I was also curious what languages are actually nice to read when contraining to stdlib only.

